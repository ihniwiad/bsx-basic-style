( function( $, Utils ) {

    // TEST – TODO: remove
    console.log( 'TEST' );
    Utils.$window.on( Utils.events.initJs, function() {
        console.log( 'initJs' );
    } );

    /*

	// init
    Utils.$window.on( Utils.events.initJs, function() {

    } );

    */

} )( jQuery, BSX_UTILS );